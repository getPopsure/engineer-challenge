import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';

/**
 * Test default values by rendering a context consumer without a
 * matching provider
 */
describe("Context Provider", () => {
  test("Get data from api", () => {
  })
  describe("Filters", () => {
    test("add filter to context", () => {
      // 
    })
    test("remove filter to context", () => {
      // 
    })
    test("clear all filters in context", () => {
      // 
    })
    test("get filteres data from api", () => {
      // 
    })
  })
  describe("Search", () => {
    test("search user by name", () => {
      // 
    })
    test("clear search field returns all users", () => {
      // 
    })
  })
  describe("Pagination", () => {
    test("switch to second page", () => {
      // 
    })
  })
})