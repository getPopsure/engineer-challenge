import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';

describe("Table", () => {
  test("renders table head", () => {
    // 
  })
  test("renders 10 results", () => {
    // 
  })
})